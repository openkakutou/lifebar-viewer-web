# Data models

No data models yet — the codebase is at scaffold stage (a single exported version constant). Lifebar element/parse types and the `sff` WASM bridge's types will be documented here as they land.
